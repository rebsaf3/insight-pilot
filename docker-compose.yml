version: '3.8'
services:
  insightpilot:
    build: .
    ports:
      - "8501:8501"  # Streamlit
      - "8100:8100"  # FastAPI
    env_file:
      - .env
    volumes:
      - ./:/app
    restart: unless-stopped
