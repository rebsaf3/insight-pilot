name: Deploy to Railway
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  ci-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: CI gate for Railway deploy
        run: echo "CI passed. Railway can deploy this commit."
